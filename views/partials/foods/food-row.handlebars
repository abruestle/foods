<tr>
    <td>
        {{this.id}}
    </td>
    <td>
        {{this.food_name}}
    </td>
    <td>
        {{this.allergens}}
    </td>
    <td>
	<button class="eat" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Remake{{else}}Eat{{/if}}
	</button>
	<button class="delete" data-id="{{this.id}}">Delete</button>
    </td>
</tr>